# Port to listen on
PORT=3000

# Public URL of this server (used in OAuth metadata)
BASE_URL=https://mcp.brag.fast

# Bragfast API base URL
BRAGFAST_API_URL=https://brag.fast/api/v1

# Path to persist registered OAuth clients across restarts
OAUTH_CLIENTS_FILE=/data/clients.json
